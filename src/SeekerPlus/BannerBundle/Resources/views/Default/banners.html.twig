
{% block body %}
	{% for banner in banners %}
		<div class="">
			<div class="cell">
				<a href="{{ path('show_banner', {'id' : banner.id }) }}">
				<img id="newImages" class="marginFrame" src="{{ url('index') }}../../{{ banner.imageurl }}" style="width: 16rem ! important; height: 7rem ! important;">	
				</a>
			</div>
		</div>
	{% endfor %}
	<div class="no-flexible cell align-center" onclick="SearchBanners2()" style=" cursor: pointer">
		<span class="mif-loop2 mif-2x " ></span>
	</div>
{% endblock %}